<p class='pull-right'>
  [<%= link_to 'Edit this timeslot', edit_timeslot_path(@timeslot)%>]
</p>

<%= render 'shared/content_title', title: @timeslot.name %>

<table class="table table-hover">
   <thead>
      <tr>
         <th class="col-md-1">Academy</th>        
         <th class="col-md-1">Centre</th>
         <th class="col-md-8">Room</th>
         <th class="col-md-3">Programme</th>
         <th class="col-md-3">Topic</th>         
         <th class="col-md-3">Date and Time</th>         
      </tr>
   </thead>
 
   <tbody>
      <% @timeslots.each do |timeslot| %>       
        <tr>       
           <td><%= topic.number %></td>
           <td><%= link_to topic.name, programme_topic_path(@programme, topic)%></td>
           <td><%= topic.status %></td>
        </tr>
      <% end %>      
   </tbody>
</table>








