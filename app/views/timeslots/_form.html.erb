<%= render 'shared/errors', obj: @timeslot %>

<div class='well'>
  <%= form_for @timeslot do |f| %>
    <div class='control-group'>
      <%= f.label :name %>
      <%= f.text_field :name %>
    </div>
    <%= f.submit(@timeslot.new_record? ? 'Create Timeslot' : 'Update Timeslot', class: 'btn btn-primary')%>
  <% end %>
</div>