<p class='pull-right'>  
  [<%= link_to 'Edit this Class', edit_intake_klass_path(@intake, @klass)%>]
</p>

<%= render 'shared/content_title', title: @klass.name %>            
    <% @klass.programmes.each do |programme| %>            
      <%= programme.name %>  
        <% @klass.topics.each do |topic| %>            
          <%= topic.number %>
          <%= topic.name %>
        <% end %>
      <% end %>            
    </div>
  </div>

